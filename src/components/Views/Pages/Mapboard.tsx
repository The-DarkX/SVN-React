import './Mapboard.css';

import { ClusterMap, MiniMap } from '../Blocks/Maps';

const Mapboard = () => {
    return (
        <div>
            <ClusterMap />
        </div>
    );
};

export default Mapboard;