import './Mapboard.css';

import { ClusterMap } from '../Blocks/Maps';

const Mapboard = () => {
    return (
        <div>
            <ClusterMap />
        </div>
    );
};

export default Mapboard;